---
layout: layouts/base.njk
section: gallery
---
<article>
  <figure style="view-transition-name: {{ imagePath | viewTransitionId }}">
    {% image "" + imagePath,
    title,
    "700" %}
  </figure>
  <aside>
    {% if tags %}
      {% for tag in tags %}
        {%- if tag != "image" -%}
          {% set tagUrl %}/album/{{ tag }}/{% endset %}
          <a href="{{ tagUrl | url }}" rel="tag">{{ tag }}</a>
        {%- endif -%}
      {% endfor %}
    {% endif %}
  </asife>
</article>